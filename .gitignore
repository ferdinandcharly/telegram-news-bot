# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Environnements virtuels
.venv/
venv/
env/

# Secrets et config locale (ne jamais publier)
.env
*.key
secrets.json
tokens.json

# Divers
*.log
.DS_Store
# Cles privees
*.pem

# Etat runtime (regenere par l'app au demarrage)
alertes.json
vus.json
sauvegardes.json

# Sauvegardes DB (donnees utilisateurs — ne jamais publier)
backups/

# Outils locaux / previews scratch (ne concernent pas le deploiement)
.claude/
preview-*.html
variants/
static/variants/
